public static void shuffleArr(int[] arr) {
  for (int i = 0; i < arr.length; i++) {
    int random = new Random().nextInt(arr.length - i) + i;
    int temp = arr[random];
    arr[random] = arr[i];
    arr[i] = temp;
  }
}
