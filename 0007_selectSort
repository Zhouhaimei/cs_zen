function selectSort(arr) {
 	for (var i = 0; i < arr.length - 1; i++) {
 		var k = i;
 		for (var j = i + 1; j < arr.length; j++) {
 			if (arr[k] > arr[j]) {
 			k = j;
 			}
 		}
 		var temp = arr[k];
 		arr[k] = arr[i];
 		arr[i] = temp;
 	}
 }
